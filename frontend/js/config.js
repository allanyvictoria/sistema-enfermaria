// Endereco da API do backend (FastAPI).
// - Enquanto testa no seu PC, deixe "http://localhost:8000".
// - Depois que o backend estiver publicado no Render (ou outro serviço),
//   troque pela URL pública dele, por exemplo:
//   API_URL: "https://enfermaria-backend.onrender.com"
window.APP_CONFIG = {
  API_URL: "http://localhost:8000"
};
